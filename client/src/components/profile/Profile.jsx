import React from 'react';

const Profile = () => {
    return (
        <div>
            <button
                type="button">Удалить аватар
            </button>
            <input type="file" placeholder="Загрузить аватар"/>
        </div>
    );
};

export default Profile;